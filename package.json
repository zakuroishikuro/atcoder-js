{
  "name": "atcoderlet",
  "license": "MIT",
  "version": "0.1.0",
  "dependencies": {
    "@types/node": "^18.8.3",
    "esbuild": "^0.15.8",
    "jest": "v28.1.3",
    "koa": "^2.13.4",
    "koa-body": "^5.0.0",
    "ts-jest": "28",
    "ts-node": "^10.9.1",
    "tstl": "^2.5.8",
    "vitest": "^0.24.0"
  },
  "scripts": {
    "serve": "node _src/serve.js",
    "jest": "jest --watch --testMatch '<rootDir>/problems/**/*.js?(x)'",
    "test": "vitest problems",
    "build:bookmarklet": "esbuild _src/_bookmarklet.js --minify-whitespace --outfile=bookmarklet.js",
    "test:note": "jest --roots note --watch"
  }
}
